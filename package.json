{
  "name": "junglinelocal-gmb",
  "private": true,
  "version": "1.0.0",
  "description": "JunglineLocal GMB landing page (static HTML)",
  "scripts": {
    "build": "echo 'Static site, no build step required'",
    "start": "npx serve ."
  }
}
